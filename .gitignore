/.env
/sql/
/public/assets/uploads/

# Composer dependencies (composer.json/composer.lock are committed, vendor/
# is regenerated by `composer install` — first added for mpdf/mpdf, see
# App\Core\Pdf and handoff.md)
/vendor/

# OSPanel-local config, not needed to run or deploy the project
/.osp/

# Project backup archive, not source
/dashboard.loc.rar
